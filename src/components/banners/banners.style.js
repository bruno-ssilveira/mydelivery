import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
    icone: {
        width: 280,
        height: 150,
    },
    banner: {
        alignItems: "center",
        justifyContent: "center",
        marginTop: 10,
        marginBottom: 10,
        paddingRight: 10,
    },
}